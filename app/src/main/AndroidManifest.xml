<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kent.tv_view_focus">

    <application
        android:name=".TvApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".feature3.Feature3Activity" />
        <activity android:name=".feature.animator.AnimatorActivity" />
        <activity android:name=".feature.grid_recyclerview.GirdRvActivity" />
        <activity android:name=".feature.keepfocus.FocusKeepActivity" />
        <activity android:name=".feature2.Feature2Activity" />
    </application>

</manifest>